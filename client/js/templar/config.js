define([
	
], function(
	
) {
	return {
		input: {
			axes: {
				horizontal: {
					negative: ['left', 'a'],
					positive: ['right', 'd']
				},
				vertical: {
					negative: ['up', 'w'],
					positive: ['down', 's']
				}
			}
		}
	};
});